<!-- RpaWinUiComponents.Demo/RpaWinUiComponents.Demo.csproj - ✅ OPRAVENÝ NETSDK1004 -->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- ✅ WinUI3 Target Framework -->
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    <UseWinUI>true</UseWinUI>
    <EnableMsixTooling>true</EnableMsixTooling>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>

    <!-- ✅ OPRAVA NETSDK1004: Force restore before build -->
    <RestoreProjectStyle>PackageReference</RestoreProjectStyle>
    <RestorePackagesWithLockFile>false</RestorePackagesWithLockFile>

    <!-- ✅ XAML nastavenia -->
    <EnableDefaultPageItems>true</EnableDefaultPageItems>
    <EnableDefaultXamlItems>true</EnableDefaultXamlItems>
    <XamlCompilationEnabled>true</XamlCompilationEnabled>
    <UseCompiledXaml>true</UseCompiledXaml>

    <!-- ✅ Assembly Info -->
    <AssemblyTitle>RPA WinUI Components Demo</AssemblyTitle>
    <AssemblyDescription>Demo aplikácia pre testovanie AdvancedWinUiDataGrid balíka</AssemblyDescription>
    <AssemblyVersion>1.0.0</AssemblyVersion>

    <!-- ✅ Platforms -->
    <Platforms>x86;x64;ARM64</Platforms>
    <RuntimeIdentifiers>win-x86;win-x64;win-arm64</RuntimeIdentifiers>

    <!-- ✅ Suppress warnings -->
    <SupportedOSPlatformVersion>10.0.17763.0</SupportedOSPlatformVersion>
    <NoWarn>$(NoWarn);CA1416;NETSDK1022;CS0414</NoWarn>

    <!-- ✅ CONDITIONAL: Ak package existuje, definuj HAS_PACKAGE -->
    <DefineConstants Condition="Exists('$(NuGetPackageRoot)rpawinuicomponents.advancedwinuidatagrid\1.0.18\')">$(DefineConstants);HAS_PACKAGE</DefineConstants>
  </PropertyGroup>

  <!-- ✅ WinUI3 Dependencies - VŽDY POTREBNÉ -->
  <ItemGroup>
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.7.250606001" />
    <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.26100.4654" />
    <PackageReference Include="RpaWinUiComponents.AdvancedWinUiDataGrid" Version="1.0.18" />
  </ItemGroup>

  <!-- ✅ CONDITIONAL: Package reference iba ak lokálne existuje package alebo je force enabled -->

  <!-- ✅ FALLBACK: Pre testovanie bez package -->
  <PropertyGroup Condition="'$(ForcePackageReference)' != 'true'">
    <PackageAvailable>false</PackageAvailable>
  </PropertyGroup>

  <!-- ✅ Assets - iba ak existujú -->
  <ItemGroup Condition="Exists('Assets')">
    <Content Include="Assets\**">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <!-- ✅ Manifest - iba ak existuje -->
  <ItemGroup Condition="Exists('Package.appxmanifest')">
    <None Include="Package.appxmanifest">
      <SubType>Designer</SubType>
    </None>
  </ItemGroup>

  <!-- ✅ Pre testovanie v development - ak package neexistuje, vytvor fake implementáciu -->
  <Target Name="CheckPackageAvailability" BeforeTargets="Build">
    <PropertyGroup>
      <LocalPackageExists Condition="Exists('$(MSBuildProjectDirectory)\..\AdvancedWinUiDataGrid\bin\Debug\RpaWinUiComponents.AdvancedWinUiDataGrid.1.0.18.nupkg')">true</LocalPackageExists>
    </PropertyGroup>
    
    <Message Text="📦 Checking package availability..." Importance="normal" />
    <Message Text="✅ Local package found" Importance="normal" Condition="'$(LocalPackageExists)' == 'true'" />
    <Message Text="⚠️ Local package not found - demo will run in limited mode" Importance="normal" Condition="'$(LocalPackageExists)' != 'true'" />
  </Target>

  <!-- ✅ Force restore target pre NETSDK1004 -->
  <Target Name="EnsureNuGetRestore" BeforeTargets="PrepareForBuild">
    <PropertyGroup>
      <AssetsFileExists Condition="Exists('$(MSBuildProjectDirectory)\obj\project.assets.json')">true</AssetsFileExists>
    </PropertyGroup>
    
    <Message Text="🔍 Checking assets file..." Importance="normal" />
    
    <Exec Command="dotnet restore &quot;$(MSBuildProjectFile)&quot; --verbosity quiet" Condition="'$(AssetsFileExists)' != 'true'" ContinueOnError="true" />
    
    <Message Text="✅ NuGet restore completed" Importance="normal" Condition="'$(AssetsFileExists)' != 'true'" />
  </Target>

  <!-- ✅ Clean target -->
  <Target Name="CleanDemoProject" BeforeTargets="Clean">
    <Message Text="🧹 Cleaning Demo project files..." Importance="normal" />
    <RemoveDir Directories="bin;obj" ContinueOnError="true" />
  </Target>

  <!-- ✅ Pre development - show build info -->
  <Target Name="ShowBuildInfo" BeforeTargets="Build" Condition="'$(Configuration)' == 'Debug'">
    <Message Text="🚀 Building Demo with package availability check..." Importance="normal" />
    <Message Text="📦 Target: $(TargetFramework)" Importance="normal" />
    <Message Text="🎯 Platform: $(Platform)" Importance="normal" />
  </Target>

</Project>