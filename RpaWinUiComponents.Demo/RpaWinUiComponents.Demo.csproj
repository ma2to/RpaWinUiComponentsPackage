<!-- RpaWinUiComponents.Demo/RpaWinUiComponents.Demo.csproj - ✅ KOMPLETNE OPRAVENÝ -->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- ✅ WinUI3 Target Framework -->
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    <UseWinUI>true</UseWinUI>
    <EnableMsixTooling>true</EnableMsixTooling>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>

    <!-- ✅ KĽÚČOVÁ OPRAVA NETSDK1022: Povoliť default items ale kontrolovane -->
    <EnableDefaultCompileItems>true</EnableDefaultCompileItems>
    <EnableDefaultEmbeddedResourceItems>true</EnableDefaultEmbeddedResourceItems>

    <!-- ✅ XAML nastavenia - povoliť default XAML items -->
    <EnableDefaultPageItems>true</EnableDefaultPageItems>
    <EnableDefaultXamlItems>true</EnableDefaultXamlItems>
    <XamlCompilationEnabled>true</XamlCompilationEnabled>
    <UseCompiledXaml>true</UseCompiledXaml>

    <!-- ✅ Assembly Info -->
    <AssemblyTitle>RPA WinUI Components Demo</AssemblyTitle>
    <AssemblyDescription>Demo aplikácia pre testovanie AdvancedWinUiDataGrid balíka</AssemblyDescription>
    <AssemblyVersion>1.0.0</AssemblyVersion>

    <!-- ✅ Platforms -->
    <Platforms>x86;x64;ARM64</Platforms>
    <RuntimeIdentifiers>win-x86;win-x64;win-arm64</RuntimeIdentifiers>

    <!-- ✅ Windows App SDK nastavenia -->
    <SupportedOSPlatformVersion>10.0.17763.0</SupportedOSPlatformVersion>
    <NoWarn>$(NoWarn);CA1416;NETSDK1022</NoWarn>
  </PropertyGroup>

  <!-- ✅ WinUI3 Dependencies -->
  <ItemGroup>
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.7.250606001" />
    <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.26100.4654" />
    <PackageReference Include="RpaWinUiComponents.AdvancedWinUiDataGrid" Version="1.0.18" />
    <!-- ✅ KĽÚČOVÉ: Package Reference na náš balík -->
  </ItemGroup>

  <!-- ✅ ODSTRÁNENÉ: Explicitné XAML a Compile definície (spoliehame sa na default items) -->
  <!-- Toto eliminuje NETSDK1022 chybu -->

  <!-- ✅ Assets - iba ak existujú -->
  <ItemGroup Condition="Exists('Assets')">
    <Content Include="Assets\**">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <!-- ✅ Manifest - iba ak existuje -->
  <ItemGroup Condition="Exists('Package.appxmanifest')">
    <None Include="Package.appxmanifest">
      <SubType>Designer</SubType>
    </None>
  </ItemGroup>

  <!-- ✅ Clean target pre development -->
  <Target Name="CleanDemoProject" BeforeTargets="Clean">
    <Message Text="🧹 Cleaning Demo project files..." Importance="normal" />
    <RemoveDir Directories="bin;obj" />
  </Target>

  <!-- ✅ Package verification target -->
  <Target Name="VerifyPackageReference" BeforeTargets="Build" Condition="'$(Configuration)' == 'Debug'">
    <Message Text="📦 Verifying AdvancedWinUiDataGrid package reference..." Importance="normal" />
  </Target>

</Project>