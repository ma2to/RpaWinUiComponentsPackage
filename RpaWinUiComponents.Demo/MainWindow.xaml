<!-- RpaWinUiComponents.Demo/MainWindow.xaml - ✅ DEMO s Individual Colors + Search/Sort/Zebra -->
<Window x:Class="RpaWinUiComponents.Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:grid="using:RpaWinUiComponents.AdvancedWinUiDataGrid"
        Title="🚀 RPA WinUI Components Demo - Individual Colors + Search/Sort/Zebra">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ✅ Header -->
        <Border Grid.Row="0" 
                Background="{ThemeResource LayerFillColorAltBrush}"
                Padding="20,16">
            <StackPanel>
                <TextBlock Text="🚀 RPA WinUI Components Demo"
                          FontSize="24"
                          FontWeight="Bold"
                          Margin="0,0,0,8"/>
                <TextBlock x:Name="InitStatusText"
                          Text="Inicializuje sa Individual Colors + Search/Sort/Zebra..."
                          FontSize="14"
                          Opacity="0.8"/>
            </StackPanel>
        </Border>

        <!-- ✅ Control Panel -->
        <ScrollViewer Grid.Row="1" 
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled">
            <StackPanel Orientation="Vertical" 
                        Padding="20,16" 
                        Spacing="16">

                <!-- ✅ Individual Colors Controls -->
                <Border Background="{ThemeResource LayerFillColorAltBrush}"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="🎨 Individual Colors Configuration"
                                  FontWeight="SemiBold"
                                  FontSize="16"/>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="ApplyLightThemeButton"
                                    Content="Light Colors"
                                    Click="OnApplyLightThemeClick"
                                    Style="{StaticResource AccentButtonStyle}"/>

                            <Button x:Name="ApplyDarkThemeButton"
                                    Content="Dark Colors"
                                    Click="OnApplyDarkThemeClick"/>

                            <Button x:Name="ApplyBlueThemeButton"
                                    Content="Blue Colors"
                                    Click="OnApplyBlueThemeClick"/>

                            <Button x:Name="ApplyCustomThemeButton"
                                    Content="Custom Orange + Strong Zebra"
                                    Click="OnApplyCustomThemeClick"/>

                            <Button x:Name="ResetThemeButton"
                                    Content="Reset (No Zebra)"
                                    Click="OnResetThemeClick"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- ✅ Search/Sort/Zebra Controls -->
                <Border Background="{ThemeResource LayerFillColorAltBrush}"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="🔍 Search/Sort/Zebra Testing"
                                  FontWeight="SemiBold"
                                  FontSize="16"/>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="TestSearchButton"
                                    Content="🔍 Test Search"
                                    Click="OnTestSearchClick"/>

                            <Button x:Name="TestSortButton"
                                    Content="⬆️⬇️ Test Sort"
                                    Click="OnTestSortClick"/>

                            <Button x:Name="TestZebraToggleButton"
                                    Content="🦓 Toggle Zebra"
                                    Click="OnTestZebraToggleClick"/>

                            <Button x:Name="ClearSearchButton"
                                    Content="🧹 Clear Search"
                                    Click="OnClearSearchClick"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- ✅ AUTO-ADD Testing -->
                <Border Background="{ThemeResource LayerFillColorAltBrush}"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="🔥 AUTO-ADD Functionality Testing"
                                  FontWeight="SemiBold"
                                  FontSize="16"/>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="TestAutoAddFewRowsButton"
                                    Content="Load 2 Rows (< minimum)"
                                    Click="OnTestAutoAddFewRowsClick"/>

                            <Button x:Name="TestAutoAddManyRowsButton"
                                    Content="Load 20 Rows (> minimum)"
                                    Click="OnTestAutoAddManyRowsClick"/>

                            <Button x:Name="TestAutoAddDeleteButton"
                                    Content="Test Smart Delete"
                                    Click="OnTestAutoAddDeleteClick"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- ✅ Standard Functions -->
                <Border Background="{ThemeResource LayerFillColorAltBrush}"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="📊 Standard DataGrid Functions"
                                  FontWeight="SemiBold"
                                  FontSize="16"/>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="LoadSampleDataButton"
                                    Content="📊 Load Sample Data"
                                    Click="OnLoadSampleDataClick"
                                    Style="{StaticResource AccentButtonStyle}"/>

                            <Button x:Name="ValidateAllButton"
                                    Content="✅ Validate All"
                                    Click="OnValidateAllClick"/>

                            <Button x:Name="ClearDataButton"
                                    Content="🗑️ Clear All Data"
                                    Click="OnClearDataClick"/>

                            <Button x:Name="ExportDataButton"
                                    Content="📤 Export Data"
                                    Click="OnExportDataClick"/>

                            <Button x:Name="DeleteByCustomValidationButton"
                                    Content="🎯 Custom Delete"
                                    Click="OnDeleteByCustomValidationClick"/>

                            <Button x:Name="AdvancedDeleteExamplesButton"
                                    Content="⚡ Advanced Delete"
                                    Click="OnAdvancedDeleteExamplesClick"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- ✅ Testing Functions -->
                <Border Background="{ThemeResource LayerFillColorAltBrush}"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="🧪 Advanced Testing Functions"
                                  FontWeight="SemiBold"
                                  FontSize="16"/>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="TestRealtimeValidationButton"
                                    Content="⚡ Realtime Validation"
                                    Click="OnTestRealtimeValidationClick"/>

                            <Button x:Name="TestNavigationButton"
                                    Content="🧭 Navigation (Tab/Enter)"
                                    Click="OnTestNavigationClick"/>

                            <Button x:Name="TestCopyPasteButton"
                                    Content="📋 Copy/Paste"
                                    Click="OnTestCopyPasteClick"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- ✅ Main DataGrid Area -->
        <Border Grid.Row="2"
                Margin="20,0,20,16"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">

            <Grid>
                <!-- ✅ Loading Panel -->
                <StackPanel x:Name="LoadingPanel"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Spacing="20"
                           Visibility="Visible">

                    <ProgressRing Width="48" 
                                 Height="48" 
                                 IsActive="True"/>

                    <StackPanel Spacing="12" 
                               HorizontalAlignment="Center">
                        <TextBlock x:Name="LoadingDetailText"
                                  Text="Inicializuje sa Individual Colors + Search/Sort/Zebra..."
                                  FontSize="16"
                                  FontWeight="Medium"
                                  HorizontalAlignment="Center"/>

                        <TextBlock Text="📦 Package Reference testovanie aktívne..."
                                  FontSize="14"
                                  Opacity="0.7"
                                  HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <!-- ✅ Main DataGrid Component -->
                <grid:AdvancedDataGrid x:Name="DataGridControl"
                                      Visibility="Collapsed"/>
            </Grid>
        </Border>

        <!-- ✅ Status Bar -->
        <Border Grid.Row="3"
                Background="{ThemeResource LayerFillColorAltBrush}"
                Padding="20,12">
            <TextBlock x:Name="StatusTextBlock"
                      Text="📦 Inicializuje sa AdvancedWinUiDataGrid s Individual Colors + Search/Sort/Zebra cez Package Reference..."
                      FontSize="12"
                      Opacity="0.8"/>
        </Border>

    </Grid>
</Window>