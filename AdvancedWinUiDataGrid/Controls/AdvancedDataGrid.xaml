<!-- AdvancedWinUiDataGrid/Controls/AdvancedDataGrid.xaml - ✅ KOMPLETNE OPRAVENÝ XAML parsing fix -->
<UserControl x:Class="RpaWinUiComponents.AdvancedWinUiDataGrid.AdvancedDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:RpaWinUiComponents.AdvancedWinUiDataGrid">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">

        <!-- ✅ Main Content Grid - Visibility controlled by code-behind -->
        <Grid x:Name="MainContentGrid" Visibility="Collapsed">

            <!-- ✅ Header row pre stĺpce -->
            <Grid x:Name="HeaderGrid" VerticalAlignment="Top" Height="40">
                <Border Background="{ThemeResource LayerFillColorAltBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="0,0,0,1">
                    <ScrollViewer x:Name="HeaderScrollViewer" 
                                  HorizontalScrollBarVisibility="Hidden" 
                                  VerticalScrollBarVisibility="Disabled"
                                  ZoomMode="Disabled">
                        <StackPanel x:Name="HeaderPanel" 
                                    Orientation="Horizontal" 
                                    Height="40"/>
                    </ScrollViewer>
                </Border>
            </Grid>

            <!-- ✅ Data rows area s auto-add podporou -->
            <ScrollViewer x:Name="DataGridScrollViewer"
                          Margin="0,40,0,0"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          ZoomMode="Disabled">

                <StackPanel x:Name="DataRowsPanel" 
                            Orientation="Vertical"
                            Spacing="0"/>

            </ScrollViewer>
        </Grid>

        <!-- ✅ Loading Overlay - Visibility controlled by code-behind -->
        <Border x:Name="LoadingOverlay"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Visibility="Visible">

            <StackPanel HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Spacing="16">

                <ProgressRing IsActive="True" 
                             Width="32" 
                             Height="32"/>

                <TextBlock x:Name="LoadingText"
                          Text="Inicializuje sa Advanced DataGrid s Auto-Add..."
                          FontSize="14"
                          FontWeight="Medium"
                          HorizontalAlignment="Center"
                          TextWrapping="Wrap"
                          MaxWidth="300"/>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>